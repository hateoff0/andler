[package]
name = "andler-helper"
version.workspace = true
edition.workspace = true
license.workspace = true

# The single privileged binary andlerd shells out to via the one NOPASSWD
# sudoers rule. Deliberately dependency-light: only `libc` (for chroot(2)).
[dependencies]
libc = "0.2"

[[bin]]
name = "andler-helper"
path = "src/main.rs"


[lints]
workspace = true
