# Virtio modules for QEMU guest (disk, network, GPU, console)
MODULES=(virtio virtio_pci virtio_ring virtio_blk virtio_scsi virtio_net virtio_gpu virtio_balloon virtio_console)

BINARIES=()
FILES=()

# systemd-based initrd (required for UKI generation via ukify)
HOOKS=(base systemd autodetect modconf kms keyboard sd-vconsole block filesystems fsck)

COMPRESSION="zstd"
