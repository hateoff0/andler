# frontend

**Не начато.** Tauri GUI-клиент. Не трогать до стабильной связки
`andler-core` + `andler-qemu` + `andler-cli` (см. рекомендацию "заморозить
скоуп" — сначала core, потом GUI).

Когда дойдёт очередь — см. `docs/architecture/FRONTEND_PLAN.md`, в частности
открытый риск инпут-лага при стриминге VNC/SPICE через WebKitGTK (раздел про
PoC рендеринга, на основе которого нужно решить Tauri vs egui до основной
разработки).
