# presets

**Не начато.** ANDLER-Proton — пресеты под конкретные игры/приложения
(TOML + JSON Schema валидация). Применяются как оверрайд уже собранного
`InstanceConfig`, не заменяют механизм создания инстанса.

Полный план: `docs/architecture/PRESETS_PLAN.md`.
